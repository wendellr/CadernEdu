# Build de imagens Docker — DESABILITADO
#
# O build agora é feito diretamente pelo Portainer ao fazer deploy da stack.
# Ver infra/docker-compose.portainer.yml (seções build:)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Desabilitado — Portainer builda direto do repo'
        required: true

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build feito pelo Portainer. Ver infra/docker-compose.portainer.yml"
