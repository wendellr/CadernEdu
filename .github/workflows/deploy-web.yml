# Deploy via Vercel — DESABILITADO
#
# O deploy usa Docker via Portainer (infra/docker-compose.portainer.yml).
# Este workflow foi substituído pelo docker-build.yml.
# Mantido aqui como referência caso Vercel seja usado no futuro.

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Desabilitado — usar docker-build.yml'
        required: true

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy via Portainer. Ver infra/docker-compose.portainer.yml"
