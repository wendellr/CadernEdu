// Testes de widget serão adicionados conforme as telas forem estabilizadas.
// Por enquanto apenas um smoke test para verificar que o projeto compila.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — projeto compila sem erros', () {
    expect(1 + 1, 2);
  });
}
